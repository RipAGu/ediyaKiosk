<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stageusproject">
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StageusProject">
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartActivity"/>
        <activity android:name=".OptionActivity"/>
        <activity android:name=".MenuListActivity"/>
        <activity android:name=".RemoveMenuActivity"/>
        <activity android:name=".PaymentActivity"/>
        <activity android:name=".OrderCompleteActivity"/>
        <activity android:name=".SignupActivity"/>
        <service android:name=".CartService"/>

    </application>

</manifest>